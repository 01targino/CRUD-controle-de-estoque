package com.controleDeEstoque.ControleDeEstoque.controllers;

import java.time.LocalDate;

import com.controleDeEstoque.ControleDeEstoque.controllers.Categoria;

public record DadosAdicionarProduto(
		String nome,
		int quantidade,
		float preco,
		Categoria categoria,
		String lote,
		LocalDate validade,
		Fornecedor fornecedor
		) {

}
